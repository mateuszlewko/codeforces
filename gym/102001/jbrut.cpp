#include <bits/stdc++.h>
#define fi first
#define se second
#define pb push_back
using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

int main()
{
  #ifndef DEBUG
  freopen("nazwa.in", "r", stdin);
  freopen("nazwa.out", "w", stdout);
  #endif
  ios_base::sync_with_stdio(0);

  int N, M;
  cin >> N >> M;

  int pola = N * M;
  
  for(int i = )


  return 0;
}